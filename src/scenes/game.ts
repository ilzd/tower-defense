import Phaser from 'phaser'

export default class GameScene extends Phaser.Scene {
  public static readonly SCENE_KEY = 'game-scene'

  constructor () {
    super(GameScene.SCENE_KEY)
  }

  preload () {
  }

  create () {
  }
}
